# Build output and generated files. Mirrors the `ignores` block in eslint.config.js.
# Without this, `format:check` passes on a clean checkout (CI order: ci -> format:check
# -> build) but fails locally the moment anyone has run a build.
dist/
coverage/
package-lock.json

# Generated by @tanstack/router-plugin. It is emitted without semicolons, so
# format:check fails on it otherwise — and reformatting it would be undone by the
# next build anyway.
src/routeTree.gen.ts
