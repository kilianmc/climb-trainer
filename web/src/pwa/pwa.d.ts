// `virtual:pwa-register` is created by the Vite plugin, so its declaration ships with the
// plugin. Only the vanillajs surface is referenced: `vite-plugin-pwa/client` pulls in the
// Vue, Preact, Svelte and Solid declarations too, none of which this app has installed.
/// <reference types="vite-plugin-pwa/vanillajs" />
