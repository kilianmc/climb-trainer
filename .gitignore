# dependencies
node_modules/
.venv/

# build output
dist/
*.tsbuildinfo

# secrets — never commit. Anything here belongs in Vercel env vars.
.env
.env.*
!.env.example

# tooling
.vercel
.pytest_cache/
.ruff_cache/
.mypy_cache/
__pycache__/
*.py[cod]
coverage/
.DS_Store

# local-only scratch: per-PR working notes, review aids, and the scripts that seed a local
# database for a visual review — tooling for the review, never a deliverable
.claude/pr-*.md
.claude/*.py
.claude/exercise-classification.md
